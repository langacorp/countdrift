{
  "claims": [
    {
      "name": "services",
      "note": "How many services we say we run, against the pages that exist.",
      "pattern": "\\b(\\d+)\\s+(?:servizi|services)\\b",
      "truth": { "type": "files", "glob": "site/services/*", "directories": true },
      "paths": ["site/**/*.php", "README.md"]
    },
    {
      "name": "agents",
      "note": "A count that used to be typed into a rulebook. Now it is read.",
      "pattern": "\\b(\\d+)\\s+(?:agenti|agents)\\b",
      "truth": { "type": "json", "file": "build/agents.json", "path": "count" },
      "paths": ["docs/**/*.md"]
    }
  ]
}
